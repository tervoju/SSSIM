init: {
	density: {
		start = 1002.5; // incremented by 0.1 * #id
		step = 0.8;
		end = 1010.0;
	};
	time: {
		max = 3600;  // was 2*60*60 s
		rise_buffer = 300;  // 5*60 s
	};
	max_depth = 100;
};

cycle: {
	period = 3600;  // was 2*60*60 s
	tx_slot_seconds = 60;
};
